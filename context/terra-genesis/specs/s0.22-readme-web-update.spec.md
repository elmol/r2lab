# S0.22 — README Update: Web Portal

> **Status:** Draft
> **Type:** Documentation sync
> **Depends on:** S2c-Web-V1 (implemented)

---

## Problem

The web portal (React app) was added in PRs #16 and #20 but the README has zero mention of it. Someone reading the README doesn't know the web exists.

---

## Changes Required

### 1. Add "Web Interface" section (after "On-Chain Verification")

```markdown
## Web Interface

TerraGenesis includes a browser-based portal for inspecting the device registry and verifying captures:

- **Registry Browser** — view all registered devices with attestation status
- **Device Registration** — register new devices on-chain (attester wallet required)
- **Capture Verification** — upload image + metadata + manifest, verify locally and on-chain

### Run locally

\```bash
cd web
npm install
npm run dev
\```

Configure via `.env` (see `web/.env.example`):
- `VITE_CONTRACT_ADDRESS` — deployed HardTrustRegistry address
- `VITE_RPC_URL` — JSON-RPC endpoint (default: `http://127.0.0.1:8545`)
- `VITE_EXPECTED_CHAIN_ID` — expected chain ID (default: `31337`)

Requires MetaMask or any injected EVM wallet.
```

### 2. Update "Repository Structure"

Add `web/` directory:

```
├── web/               # React frontend — registry portal + capture verifier
│   ├── src/
│   └── package.json
```

### 3. Update "Hackathon Scope"

Add checkbox:
```
- [x] Web-based registry portal and capture verification UI
```

### 4. Fix branding (separate concern, can be done in web code)

- Header title: "HardTrust" → "TerraGenesis"
- Tagline: update to reflect TerraScope/DeSci focus

### 5. Update Architecture diagram

Add web portal as a fourth column or expand the "Verify Anyone" column to mention the web UI.

---

## Acceptance Criteria

- [ ] README mentions the web app with run instructions
- [ ] `web/` appears in repo structure
- [ ] Hackathon scope updated
- [ ] Reader can find and run the web app from README alone
