# Handoff: S0.2 — CI Pipeline

## Pre-requisites

- S0.1 implemented (repo exists)
- Copy `s0.2-ci-pipeline.spec.md` to `docs/specs/` in HardTrust before running

## Instructions

1. Open Claude Code in the HardTrust repo
2. Paste the prompt below

## Prompt

```
Read the spec at docs/specs/s0.2-ci-pipeline.spec.md and implement it.

Create a GitHub Actions CI workflow. Only what the spec says — nothing more.

Validate: `just ci` passes locally. Push to a branch and open a PR to verify CI runs.
```

## After execution

1. Verify CI runs on the PR
2. Merge the PR
3. Configure branch protection on `main` via GitHub UI:
   - Require status checks to pass (lint, test)
   - Require 1 approval
   - No direct push
4. Return to r2lab with feedback
