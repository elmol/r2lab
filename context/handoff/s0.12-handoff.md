# Handoff: S0.12 — Release Pipeline v2

## Context

Refactor del pipeline de release: extraer build logic a scripts reutilizables y separar install.sh en dos scripts focalizados.

## Spec

Read `docs/specs/s0.12-release-pipeline-v2.spec.md` — contiene el contenido exacto de todos los archivos.

## Task (un solo branch: `refactor/s0.12-release-pipeline-v2`)

### 1. Crear `scripts/build-device.sh`
Contenido completo en la spec. Hacer ejecutable: `git update-index --chmod=+x scripts/build-device.sh`

### 2. Crear `scripts/build-attester.sh`
Contenido completo en la spec. Hacer ejecutable: `git update-index --chmod=+x scripts/build-attester.sh`

### 3. Crear `install-device.sh` en repo root
Contenido completo en la spec. Reemplazar `OWNER` con el GitHub username real (`git remote get-url origin`). Hacer ejecutable: `git update-index --chmod=+x install-device.sh`

### 4. Crear `install-attester.sh` en repo root
Contenido completo en la spec. Reemplazar `OWNER`. Hacer ejecutable: `git update-index --chmod=+x install-attester.sh`

### 5. Actualizar `.github/workflows/release.yml`
En los jobs `build-device-armv7`, `build-attester-linux`, `build-attester-macos`: reemplazar los steps inline de build (Build contracts + Build binary + Rename and checksum) por un solo step que llama al script:
- `build-device-armv7` → `run: bash scripts/build-device.sh`
- `build-attester-linux` → `run: bash scripts/build-attester.sh`
- `build-attester-macos` → `run: bash scripts/build-attester.sh`

En el job `release`, actualizar las release notes para apuntar a `install-device.sh` e `install-attester.sh`.

### 6. Agregar a `justfile`
```justfile
# Cross-compile device for ARMv7 (mirrors CI exactly)
build-device-release:
    RELEASE_VERSION=dev bash scripts/build-device.sh

# Build attester for current host (mirrors CI exactly)
build-attester-release:
    RELEASE_VERSION=dev bash scripts/build-attester.sh
```

### 7. Eliminar `install.sh`
`git rm install.sh`

### Validar
- `RELEASE_VERSION=dev bash scripts/build-attester.sh` produce `attester-dev-x86_64-unknown-linux-gnu`
- `bash install-device.sh` en Ubuntu → mensaje de error claro con instrucción de usar `install-attester.sh`
- `just ci` pasa
- Push test tag y verificar que el pipeline sigue produciendo los 3 binarios

## Branch
`refactor/s0.12-release-pipeline-v2`

## Acceptance
- `scripts/build-device.sh` y `scripts/build-attester.sh` son ejecutables y reproducen el build de CI
- `install-device.sh` e `install-attester.sh` fallan con mensajes claros en plataforma incorrecta
- `install.sh` eliminado
- Pipeline actualizado y funcional
