# Handoff: S0.6 — HardTrust CLAUDE.md Updates for Slice 1

## Pre-requisites

- S0.5 implemented (CLAUDE.md exists with AI dev practices)
- Spec already copied to `docs/specs/s0.6-hardtrust-claude-md-updates.spec.md` in HardTrust

## Instructions

1. Open Claude Code in the HardTrust repo
2. Switch to branch `docs/stories-and-specs`
3. Paste the prompt below

## Prompt

```
Update HardTrust CLAUDE.md with build order and local development context.

Read the spec at docs/specs/s0.6-hardtrust-claude-md-updates.spec.md first.

Only modify CLAUDE.md — add the two new sections (Build Order and Local Development).
Do not change any other files.
Respect the "What NOT to Add" section strictly.
Branch: docs/stories-and-specs (continue on existing branch)
```

## After execution

1. Verify CLAUDE.md contains "Build Order" section with forge build → cargo build order
2. Verify CLAUDE.md contains "Local Development (Anvil)" section with account table
3. Verify it mentions that Anvil keys are NOT secrets
4. Commit and push
5. Return to r2lab with feedback
