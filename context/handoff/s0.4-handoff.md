# Handoff: S0.4 — Seed ADRs

## Pre-requisites

- S0.3 implemented (CLAUDE.md has ADR rules)
- Copy `s0.4-seed-adrs.spec.md` to `docs/specs/` in HardTrust before running

## Instructions

1. Open Claude Code in the HardTrust repo
2. Paste the prompt below

## Prompt

```
Read the spec at docs/specs/s0.4-seed-adrs.spec.md and implement it.

Create 6 seed ADRs in docs/adr/ following the template in the spec.
Each ADR captures a decision already made — do not invent new decisions.

Use the rationale from the spec. Keep each ADR concise (1-2 paragraphs per section).

Validate: 6 files exist, each follows the template, all marked "Accepted".
Commit and open a PR.
```

## After execution

1. Review each ADR — does the rationale match the architecture decisions?
2. Merge the PR
3. Return to r2lab with feedback — ready for S1a.1 (first functional story)
