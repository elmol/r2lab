# Handoff: S0.7 — Solidity Static Analysis

## Pre-requisites

- S0.6 implemented (CLAUDE.md has build order)
- V1 implemented (HardTrustRegistry.sol exists to lint)
- `npm` available (for solhint)
- `cargo install aderyn` run locally

## Instructions

1. Open Claude Code in the HardTrust repo
2. Paste the prompt below

## Prompt

```
Add Solidity static analysis (Solhint + Aderyn) to HardTrust.

Read the spec at docs/specs/s0.7-solidity-static-analysis.spec.md first.

Output files:
- contracts/.solhint.json
- contracts/package.json (for solhint)
- Updated justfile (add solhint to lint recipe)
- Updated .github/workflows/ci.yml (add Solhint to lint job)
- Fix any findings in contracts/src/HardTrustRegistry.sol

Install aderyn locally (cargo install aderyn) and run it, but do NOT add it to CI yet.
Respect the "What NOT to Build" section strictly.
After implementation, run `just ci` to validate everything passes.
Branch: feat/s1a.1-register-device (continue on existing branch)
```

## After execution

1. Verify `npx solhint 'src/**/*.sol'` passes with no errors in contracts/
2. Verify HardTrustRegistry.sol uses custom errors (no require strings)
3. Verify `aderyn .` reports no high/critical in contracts/
4. Verify `just ci` passes
5. Commit and push
6. Return to r2lab with feedback
