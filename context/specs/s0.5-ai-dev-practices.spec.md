# Spec: S0.5 — AI Development Practices

## Goal

Upgrade HardTrust's CLAUDE.md, REVIEW.md, and project configuration so that Claude Code has full context when implementing stories and follows proven AI-assisted development patterns.

S0.3 established the initial rules. This spec closes the gaps discovered after the first implementation cycles: missing story context loading, no cross-referencing between docs, no structured error-handling workflow, and no skills/tools guidance.

## Problem Statement

1. CLAUDE.md says "Read the spec" but never mentions user stories. Stories contain acceptance criteria, edge cases, and dependencies that Claude Code needs.
2. No defined order for context loading (what to read first, what to read when).
3. No guidance for how Claude Code should handle CI failures, test failures, or review feedback loops.
4. No skills installed for Solidity security analysis.
5. REVIEW.md lacks cross-referencing rules (checking implementation against both spec AND story acceptance criteria).
6. No branch naming convention defined.

## What to Build

### 1. CLAUDE.md — Context Loading Section

Add a new section **before** the operational rules. This tells Claude Code exactly what to read and when.

```markdown
## Context Loading

Before implementing any story, read these documents in order:

1. **Story:** `docs/stories/slice-N/[story-id]-[name].md` — acceptance criteria, edge cases, dependencies
2. **Spec:** `docs/specs/[spec-id].spec.md` — what to build, what NOT to build, validation criteria
3. **Architecture:** `docs/architecture.md` — system boundaries, component responsibilities, data flow
4. **ADRs:** scan `docs/adr/` for any ADR relevant to the component you are modifying
5. **REVIEW.md** — review criteria that your PR will be checked against

For non-functional (S0.x) specs that have no story, skip step 1.

When a spec references other specs or stories (e.g., "depends on S1a.1"), read those too.
```

### 2. CLAUDE.md — Updated Operational Rules

Replace the current operational rules section with an expanded version:

```markdown
## Operational Rules

### Planning
- Read context documents (see Context Loading above) before writing any code
- Break the story into sub-tasks if it touches multiple crates
- Identify which crate(s) and files will be created or modified

### Implementation
- One story per branch, one PR per story
- Branch naming: `feat/[story-id]-short-description` (e.g., `feat/s1.1-device-init`), or `fix/[story-id]-description`, `chore/description`
- Commit granularly — one commit per logical change, not one giant commit per story
- Conventional commits: `feat:`, `fix:`, `chore:`, `docs:`, `test:`, `refactor:`
- Run `just ci` before creating a PR
- Use `--emulate` flag or `HARDTRUST_EMULATE=1` for development/CI without RPi hardware

### Quality
- Tests are part of the story — write tests alongside implementation, not after
- Every acceptance criterion in the story should have at least one test
- No `.unwrap()` in library code (`common/`) — use `Result` with descriptive errors
- All public functions must have doc comments

### Architecture Decisions
- Create an ADR in `docs/adr/` when choosing between viable alternatives or deviating from architecture
- ADRs are committed alongside the code change that implements the decision

### Boundaries
- Never merge autonomously — always require human approval
- No direct pushes to main — use PRs
- Do not implement anything listed in the spec's "What NOT to Build" section
```

### 3. CLAUDE.md — Error Handling Workflow Section

Add a new section that defines how Claude Code should handle failures:

```markdown
## When Things Fail

### CI failure
1. Read the full CI output
2. Identify the failing gate (lint, test, integration, etc.)
3. Fix the issue locally
4. Run `just ci` to verify the fix
5. Commit the fix with `fix: [description of what broke]`
6. If stuck after 3 attempts, report the failure with diagnosis to the human

### Test failure
1. Read the test output and identify the failing assertion
2. Determine if the bug is in the code or the test
3. If in the code: fix and verify
4. If in the test: check the spec/story — does the test match the acceptance criteria?
5. Run the full test suite to check for regressions

### Review feedback
1. Read all review comments
2. Fix each issue in a separate commit (or group related fixes)
3. Do not force-push — add fixup commits so the reviewer can see what changed
4. Run `just ci` after all fixes
```

### 4. CLAUDE.md — Cross-References Section

```markdown
## Documentation Cross-References

- Specs reference their source story via a "Story Reference" link at the top
- Specs and stories should reference relevant ADRs (e.g., "See ADR-0002 for why secp256k1")
- Before implementing, read any ADR referenced by the spec or story — it contains the rationale behind technical decisions
- When implementing, always verify your code satisfies BOTH the spec's validation criteria AND the story's acceptance criteria
- If a spec and story contradict each other, the spec takes precedence (specs are more recent and more detailed)
- If an implementation decision contradicts an existing ADR, do not proceed — report it to the human
- Report any contradictions to the human rather than guessing
```

### 5. REVIEW.md — Expanded Review Criteria

Replace the current REVIEW.md with:

```markdown
# REVIEW.md

## Spec and Story Compliance
- Does the implementation satisfy all acceptance criteria from the user story?
- Does the implementation match the spec's "What to Build" section?
- Does the implementation avoid everything in the spec's "What NOT to Build" section?
- Are all edge cases from the story handled?

## Code Quality (Rust)
- No `.unwrap()` in library code (`common/`) — use `Result` with descriptive errors
- `.unwrap()` is acceptable in tests and in top-level CLI error handling (with `.expect("message")`)
- All public functions have doc comments
- Error messages are actionable — they tell the user what went wrong and what to do

## Code Quality (Solidity)
- No hardcoded private keys or secrets
- Access control on state-changing functions
- Events emitted for all state changes
- NatSpec comments on public/external functions

## Security
- No secrets (private keys, mnemonics, API keys) in code or committed files
- File permissions set correctly for key material (0o600 for files, 0o700 for directories)
- Input validation on all external inputs (CLI args, contract function parameters)

## Testing
- Tests exist for new functionality
- Each acceptance criterion has at least one corresponding test
- Edge cases from the story are covered
- Tests run in CI (emulation mode where needed)

## Commits and PR
- Conventional commit messages on all commits
- PR description references the story ID and spec ID
- Changes are scoped to the story — no unrelated modifications
```

### 6. Skills — Trail of Bits (Solidity Security)

Install the `building-secure-contracts` skill from Trail of Bits when the first Solidity code lands (S1a.1-V1). Do NOT install it in this spec — there is no Solidity code to analyze yet.

**Document in CLAUDE.md** (under a "Skills" section) what is available for future use:

```markdown
## Available Skills

Install skills as needed — do not preload skills for code that does not exist yet.

| Skill | Install When | Purpose |
|-------|-------------|---------|
| `trailofbits/building-secure-contracts` | First Solidity PR (S1a.1-V1+) | Smart contract vulnerability scanning |
| `trailofbits/static-analysis` | When codebase is large enough for SARIF analysis | Multi-tool static analysis |
```

### 7. MCP Configuration

No MCP servers are needed for Slice 1. All interactions are local (filesystem, Anvil, CLI). Document this explicitly so future sessions do not waste time investigating:

Add to CLAUDE.md:

```markdown
## MCP Servers

No MCP servers configured. All Slice 1 development uses local tools only (Foundry, Cargo, just).
```

## What NOT to Build

- `.claude/settings.json` — not needed until MCP servers are added
- Commitlint or any commit-message validation tooling — CLAUDE.md rules are sufficient
- GitHub Actions for Claude Code Review — install when PRs have functional code to review
- claude-code-security-review Action — install alongside Trail of Bits skills at S1a.1-V1+
- Custom Claude Code skills or agents in the HardTrust repo
- Any automation that enforces these rules programmatically — the rules are for Claude Code's context window, not for tooling

## Validation

- [ ] CLAUDE.md has a "Context Loading" section that lists stories before specs
- [ ] CLAUDE.md operational rules include branch naming convention
- [ ] CLAUDE.md has a "When Things Fail" section covering CI, test, and review failures
- [ ] CLAUDE.md has a "Documentation Cross-References" section
- [ ] CLAUDE.md has "Available Skills" and "MCP Servers" sections
- [ ] REVIEW.md checks acceptance criteria from stories (not just spec compliance)
- [ ] REVIEW.md has separate sections for Rust, Solidity, security, and testing
- [ ] `just ci` still passes after changes (no code changes, just docs)

## Handoff Prompt

```
Update the AI development practices for HardTrust.

Read the spec at docs/specs/s0.5-ai-dev-practices.spec.md first.

Update CLAUDE.md with: context loading order, expanded operational rules,
error handling workflow, cross-reference guidance, available skills, MCP status.

Replace REVIEW.md with expanded review criteria covering story compliance,
Rust quality, Solidity quality, security, testing, and PR conventions.

Run `just ci` to verify nothing is broken. Commit and open a PR.
```
