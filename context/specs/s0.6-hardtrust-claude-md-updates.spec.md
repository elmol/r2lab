# Spec: S0.6 — HardTrust CLAUDE.md Updates for Slice 1

## Goal

Update HardTrust's CLAUDE.md with build prerequisites and local development context needed before S1a.1 implementation begins.

## What to Add

### Build Order section

Add to CLAUDE.md:

```markdown
## Build Order

Foundry contracts must be built before Rust crates that depend on ABI output:

1. `cd contracts && forge build` — generates `contracts/out/`
2. `cargo build --workspace` — Alloy `sol!` macro reads from `contracts/out/`

If you see "file not found" errors from `sol!`, run `forge build` first.
```

### Local Development section

Add to CLAUDE.md:

```markdown
## Local Development (Anvil)

The walking skeleton uses Anvil (Foundry's local chain) with deterministic accounts:

| Account | Role     | Address                                    | Private Key                                                        |
|---------|----------|--------------------------------------------|--------------------------------------------------------------------|
| #0      | Deployer | 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266 | 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80 |
| #1      | Attester | 0x70997970C51812dc3A010C7d01b50e0d17dc79C8 | 0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d |

These are well-known Anvil test keys — NOT secrets. Do not flag them as security issues.
```

## What NOT to Add

- No Alloy version pinning (let Cargo.toml be the source of truth)
- No CI changes (S0.2 already covers CI)
- No new files — only modify existing CLAUDE.md

## Validation

```bash
# After update, CLAUDE.md should contain:
grep "Build Order" CLAUDE.md
grep "Local Development" CLAUDE.md
grep "sol!" CLAUDE.md
```

## Handoff Prompt

```
Update HardTrust CLAUDE.md with build order and local development context.

Read the spec at docs/specs/s0.6-hardtrust-claude-md-updates.spec.md first.

Only modify CLAUDE.md — add the two new sections. Do not change any other files.
Respect the "What NOT to Add" section strictly.
Branch: docs/stories-and-specs (continue on existing branch)
```
